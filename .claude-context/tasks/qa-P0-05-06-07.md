# QA Report: P0-05, P0-06, P0-07

**Kontrolor:** Claude Agent (KONTROLOR)
**Datum:** 2026-04-04
**Typ kontroly:** Simplify + Debug + Reverzni

---

## P0-05 — Odkazy pravnich stranek v paticce

### Reverzni kontrola (plan vs. implementace)

| Pozadavek z planu | Stav | Poznamka |
|---|---|---|
| MainFooter: 3 odkazy (OP, GDPR, Reklamacni rad) | PASS | Radky 99-107: `/ochrana-osobnich-udaju`, `/obchodni-podminky`, `/reklamacni-rad` |
| InzerceFooter: 3 odkazy pres `urls.main(...)` | PASS | Radky 95-103: `urls.main("/ochrana-osobnich-udaju")` atd. |
| ShopFooter: 3 odkazy pres `urls.main(...)` | PASS | Radky 95-103: shodne s InzerceFooter |
| MarketplaceFooter: 3 odkazy pres `urls.main(...)` | PASS | Radky 90-98: shodne |
| WebFooter (bonus, nebyl v planu) | PASS | Radky 104-121: `/ochrana-osobnich-udaju`, `/obchodni-podminky`, `/reklamacni-rad` pres Link |
| Zadny pravni odkaz na `/kontakt` | PASS | Grep potvrdil: `/kontakt` zbyva jen v `plainText` polozkach ("Praha, CR"), NE jako pravni odkaz |

### Simplify kontrola

| Polozka | Hodnoceni | Poznamka |
|---|---|---|
| Duplicita kodu mezi footery | INFO | 5 footeru se strukturalne opakuje. Zvazit sdileny `LegalLinks` komponent (nizka priorita, neresi se ted). |
| MainFooter pouziva `<Link>` | OK | Spravne — main domena, interni navigace |
| Inzerce/Shop/Marketplace pouzivaji `<a>` + `urls.main(...)` | OK | Spravne — cross-domena, external odkaz |
| WebFooter pouziva `<Link>` | OK | Web footer je na hlavni domene, takze `<Link>` je spravny |

### Debug kontrola

- **Zadne runtime chyby** — ciste JSX, zadne podminene logiky co by mohly selhat
- **URL format konzistentni** — vsechny 3 URL spravne bez diakritiky

### Verdikt P0-05: PASS

---

## P0-06 — Odstraneni hardcoded hesla

### Reverzni kontrola (plan vs. implementace)

| Pozadavek z planu | Stav | Poznamka |
|---|---|---|
| Odstranit `const SITE_PASSWORD = "Admin2026"` | PASS | Radek 86: puvodni top-level const odstranen |
| Heslo z `process.env.SITE_PASSWORD \|\| null` | PASS | Radek 108: `const sitePassword = process.env.SITE_PASSWORD \|\| null;` — dynamicky uvnitr funkce |
| Pridat `SITE_PASSWORD &&` do podminky | PASS | Radek 109: `if (sitePassword && !shouldSkipSiteAuth(pathname))` |
| Bez env = web verejny | PASS | Logika overena: `null && ...` = false, blok se preskoci |
| Gate stranka upravit | N/A | Implementator zjistil, ze gate stranka jako soubor neexistuje. Middleware na ni redirectuje, ale bez SITE_PASSWORD se redirect nikdy nestane. Akceptovatelne pro ted. |
| Pridat do .env.example | PASS | Overeno v P0-07: radek 66 `.env.example` obsahuje `SITE_PASSWORD=` |
| Testy upraveny | PASS | `beforeEach` nastavuje `process.env.SITE_PASSWORD`, `afterEach` maze, novy test "bez SITE_PASSWORD v env je web verejny" pridan |

### Simplify kontrola

| Polozka | Hodnoceni | Poznamka |
|---|---|---|
| Dynamicke cteni uvnitr funkce vs. top-level | OK | Spravne rozhodnuti — top-level const by se nacetl jednou pri importu a znemoznil testovani. |
| `\|\| null` vs `?? null` | INFO | `\|\| null` funguje spravne — prazdny string `""` se tez vyhodnoti jako falsy, takze `SITE_PASSWORD=""` = verejny web. To je konzistentni s komentarem v .env.example. |

### Debug kontrola

| Test | Stav | Poznamka |
|---|---|---|
| Grep na "Admin2026" v zdrojovem kodu | PASS | V `middleware.ts` NENI. Zbyva jen v: plan souborech (.claude-context), TASK-QUEUE.md, a testech (jako ocekavana hodnota) — to je spravne |
| Zadny hardcoded password v produkci | PASS | middleware.ts radek 108 cte vyhradne z env |
| Testy spustit (vitest) | SKIP | `node_modules` nejsou nainstalovany (sitovy problem pri `npm install`). Testy NELZE SPUSTIT. |

### Bezpecnostni poznamky

- **UPOZORNENI:** `"Admin2026"` se stale vyskytuje v `TASK-QUEUE.md` (radky 5085, 5098, 5112, 5867) jako reference na produkci. Po launchi by se mel TASK-QUEUE procistit.
- **UPOZORNENI:** V testech (`__tests__/middleware.test.ts`) je `Admin2026` pouzit jako testovaci hodnota (16x). To je akceptovatelne — test musi pouzit nejakou hodnotu. Ale zvazit zmenu na genericke `test-password-123` pro cistotu.

### Verdikt P0-06: PASS (testy nespusteny — nutno overit po npm install)

---

## P0-07 — .env.example

### Reverzni kontrola (plan vs. implementace)

| Pozadavek z planu | Stav | Poznamka |
|---|---|---|
| `.env.example` existuje | PASS | Soubor vytvoren, 76 radku |
| Vsechny env promenne z kodu | PASS | Grep overil: 32 env promennych v kodu, vsech 32 je v .env.example |
| Zadne skutecne hodnoty | PASS | Overeno: zadne API klice, hesla, tokeny. Pouze fallback defaults (localhost URL, KOMBCZPP, "info@carmakler.cz", Cebia API base URL) |
| `.gitignore` ma `!.env.example` | PASS | Radek 34-35: `.env*` nasledovano `!.env.example` |
| `.env.local` zustava ignorovan | PASS | Pravidlo `.env*` matchuje `.env.local` |
| Komentare vysvetluji promenne | PASS | Kazda sekce ma komentar s popisem |
| SITE_PASSWORD zahrnut (navaznost P0-06) | PASS | Radek 66: `SITE_PASSWORD=` s komentarem |

### Simplify kontrola

| Polozka | Hodnoceni | Poznamka |
|---|---|---|
| Extra promenne v .env.example (ne v kodu) | OK | 5 promennych zatim nepouzitych: ANTHROPIC_API_KEY, SENTRY_DSN, SENTRY_AUTH_TOKEN, NEXT_PUBLIC_PLAUSIBLE_DOMAIN, NEXT_PUBLIC_GA4_MEASUREMENT_ID. Vsechny oznaceny jako "volitelne" — priprava na budouci features. Akceptovatelne. |
| DATABASE_URL placeholder | INFO | `postgresql://user:password@localhost:5432/carmakler` — obsahuje "password" jako placeholder text. To je standardni praxe, ale jasne komentovano. OK. |
| Struktura souboru | OK | Logicke sekce, oddelene komentari. Prehledne. |

### Debug kontrola

| Test | Stav | Poznamka |
|---|---|---|
| `.env.example` neni ignorovan gitem | PASS | `.gitignore` radek 35: `!.env.example` — vyjimka funguje |
| `.env.local` je ignorovan | PASS | Pattern `.env*` na radku 34 matchuje |
| Zadne tajne hodnoty | PASS | Vsechny API key polozky jsou prazdne |

### Verdikt P0-07: PASS

---

## Souhrnny verdikt

| Task | Simplify | Debug | Reverzni | Celkem |
|---|---|---|---|---|
| P0-05 (Footer odkazy) | PASS | PASS | PASS | **PASS** |
| P0-06 (Hardcoded heslo) | PASS | PASS (testy nespusteny) | PASS | **PASS** (s vyminkou) |
| P0-07 (.env.example) | PASS | PASS | PASS | **PASS** |

### Blokovaci problemy: ZADNE

### Doporuceni (nizka priorita, neni treba resit ted):

1. **P0-05:** Zvazit sdileny `<LegalLinks />` komponent pro footer DRY refactor (5 footeru opakuje stejne 3 odkazy)
2. **P0-06:** Zmenit testovaci heslo v `__tests__/middleware.test.ts` z `Admin2026` na `test-password-123` pro cistotu (nyni neni bezpecnostni problem, ale zmatecne pro code review)
3. **P0-06:** Procistit `TASK-QUEUE.md` od referenci na `Admin2026` pred launchem
4. **P0-06:** Spustit testy po vyreseni `npm install` sitoveho problemu — `npx vitest run __tests__/middleware.test.ts`
5. **P0-07:** Zadne

---

**Kontrola dokoncena:** 2026-04-04
**Celkovy stav:** VSECHNY 3 TASKY PASS
