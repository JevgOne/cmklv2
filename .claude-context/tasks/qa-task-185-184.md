# QA Task #185 — #184 TASK-020 Eshop autodíly gap-fix

**Commits:** A–G (`9dfadde` → `1b539a3`) na `main`, nepushnuté
**QA agent:** KONTROLOR
**Datum:** 2026-04-09
**Ref impl:** `.claude-context/tasks/impl-task-184-182.md`
**Ref plan:** `.claude-context/tasks/plan-task-182-eshop-dily-gap.md`

---

## §1 — Scope

| Commit | Oblast | QA |
|--------|--------|-----|
| A (`9dfadde`) | Schema + migration — manufacturer/warranty/WHOLESALE_SUPPLIER | ✅ |
| B (`5c13bbd`) | API + validators + middleware | ✅ |
| C (`04ce6ae`) | PWA wizard manufacturer + warranty inputs | ✅ |
| D (`776ff72`) | Web detail render + katalog filter | ✅ |
| E (`ab58f27`) | Seed WHOLESALE_SUPPLIER + 3 sample parts | ✅ |
| F (`335886d`) | E2E parts-wholesale.spec.ts | ✅ |
| G (`1b539a3`) | /simplify cleanup | ✅ |

---

## §2 — Check 1: Commit A — Schema + Migration

| Item | Implementace | Status |
|------|-------------|--------|
| `Part.manufacturer String?` | Schema L906: `manufacturer String? // Např. "TRW", "Bosch", "LUK"` | ✅ |
| `Part.warranty String?` | Schema L907: `warranty String? // Např. "24 měsíců", "zákonná", "doživotní"` | ✅ |
| `@@index([manufacturer])` | Schema L958 | ✅ |
| User.role comment update | L21: `WHOLESALE_SUPPLIER` přidán do role comment (string marker, NE Prisma enum) | ✅ |
| Migration SQL — jen ALTER + CreateIndex | `ALTER TABLE "Part" ADD COLUMN "manufacturer" TEXT, ADD COLUMN "warranty" TEXT;` + `CREATE INDEX "Part_manufacturer_idx"` — žádné tsvector/trgm manipulace | ✅ |
| Migration bez DROP INDEX artefaktů | Cleaned migration — žádné bogus DROP INDEX statements | ✅ |
| `npx prisma validate` | Schema valid 🚀 (ověřeno lokálně) | ✅ |

**Migration soubor:** `prisma/migrations/20260409062848_add_part_manufacturer_warranty/migration.sql` — 6 řádků, čistý.

---

## §3 — Check 2: Commit B — API + validators + middleware

| Item | Implementace | Status |
|------|-------------|--------|
| `createPartSchema.manufacturer` | `z.string().max(100, "Výrobce: max 100 znaků").optional()` | ✅ |
| `createPartSchema.warranty` | `z.string().max(50, "Záruka: max 50 znaků").optional()` | ✅ |
| `partFilterSchema.manufacturer` | `z.string().optional()` | ✅ |
| `api/parts/route.ts` allowedRoles | `["PARTS_SUPPLIER", "WHOLESALE_SUPPLIER", "PARTNER_VRAKOVISTE", "ADMIN", "BACKOFFICE"]` | ✅ |
| POST persist manufacturer/warranty | L43-44: `manufacturer: data.manufacturer ?? null, warranty: data.warranty ?? null` | ✅ |
| GET ILIKE filter | L105-106: `where.manufacturer = { contains: filters.manufacturer, mode: "insensitive" }` | ✅ |
| GET search OR extension | L134: `{ manufacturer: { contains: filters.search, mode: "insensitive" } }` přidán do OR | ✅ |
| `api/parts/import/route.ts` CsvRow | Interface: `manufacturer?: string; warranty?: string` | ✅ |
| Import allowedRoles | L61: WHOLESALE_SUPPLIER přidán | ✅ |
| Import headers.indexOf pattern | L104-105: `values[headers.indexOf("manufacturer")]`, `values[headers.indexOf("warranty")]` | ✅ |
| Import create persist | L151-152: `manufacturer: row.manufacturer \|\| null, warranty: row.warranty \|\| null` | ✅ |
| `middleware.ts` PARTS_SUPPLIER_ROLES | L16: `["PARTS_SUPPLIER", "WHOLESALE_SUPPLIER", "PARTNER_VRAKOVISTE", "ADMIN", "BACKOFFICE"]` | ✅ |

---

## §4 — Check 3: Commit C — PWA Wizard

| Item | Implementace | Status |
|------|-------------|--------|
| `DetailsStep.tsx` — manufacturer Input | L170-176: `Input label="Výrobce dílu (nepovinné)"`, `maxLength={100}`, vždy viditelný (bez podmínky) | ✅ |
| `PricingStep.tsx` — warranty Input | L134-140: `Input label="Záruka (nepovinné)"`, `placeholder="např. 24 měsíců, zákonná, doživotní"`, `maxLength={50}`, vždy viditelný | ✅ |
| Q5 honored — VŽDY zobrazit, bez conditional | Oba Inputs bez `{condition && <Input...>}` wrapper — unconditional render | ✅ |
| `new/page.tsx` initial state | `manufacturer: ""`, `warranty: ""` v initialDetails/initialPricing | ✅ |
| Submit body | `manufacturer: details.manufacturer \|\| undefined`, `warranty: pricing.warranty \|\| undefined` (empty string → undefined → API null) | ✅ |

---

## §5 — Check 4: Commit D — Web

| Item | Implementace | Status |
|------|-------------|--------|
| `dily/[slug]/page.tsx` — render block | Container podmínka `(part.manufacturer \|\| part.warranty)` (Q5: schovaný jen pokud obojí null) | ✅ |
| Manufacturer conditional render | `{part.manufacturer && ...}` uvnitř bloku | ✅ |
| Warranty conditional render | `{part.warranty && ...}` uvnitř bloku | ✅ |
| `dily/katalog/page.tsx` — manufacturer filter | State `const [manufacturer, setManufacturer] = useState("")`, URLSearchParams.set, ILIKE přes API | ✅ |

---

## §6 — Check 5: Commit E — Seed

| Item | Implementace | Status |
|------|-------------|--------|
| WHOLESALE_SUPPLIER user | `velkoobchod@carmakler.cz` / `heslo123`, role: `WHOLESALE_SUPPLIER`, company: "Auto Kelly Test s.r.o." | ✅ |
| 3 sample aftermarket parts | TRW (manufacturer) + "24 měsíců" warranty; Bosch + "zákonná"; Sachs + "24 měsíců" | ✅ |
| Seed je APPEND-ONLY | Blok začíná L1757 (`console.log("Seeding wholesale supplier...")`), nový, nesahá existing blocks | ✅ |
| Console login output | L2836: `console.log("Wholesale login: velkoobchod@carmakler.cz / heslo123")` | ✅ |

---

## §7 — Check 6: Commit F — E2E

| Item | Implementace | Status |
|------|-------------|--------|
| `e2e/parts-wholesale.spec.ts` existuje | NEW soubor, 123 LOC | ✅ |
| Headed Chromium pattern | `test(...)` s `page.goto`, `page.fill`, `page.screenshot` — konsistentní s ostatními E2E | ✅ |
| T1: WHOLESALE login + /parts gating | Login + goto `/parts/profile`, expect URL contains "parts/profile" | ✅ |
| Manufacturer filter + detail render testy | 4 headed Chromium tests per impl report | ✅ |

---

## §8 — Check 7: Commit G — Cleanup

| Item | Status |
|------|--------|
| Drop narrating task refs ze seed log | ✅ — `prisma/seed.ts` task reference stripped |
| Drop narrating task refs ze schema comment | ✅ — `prisma/schema.prisma` comment cleanup |
| Žádné nové bugy zavedeny | Cleanup-only changes, mechanicky ověřitelné přes diff | ✅ |

---

## §9 — Check 8: Protected files

```
git diff main~7..main -- app/api/stripe/webhook/route.ts lib/stripe-connect-shared.ts
  app/(pwa)/makler/** app/(web)/inzerce/** app/(web)/nabidka/**
  app/api/orders/** app/(web)/dily/kosik/** app/(web)/dily/objednavka/**
```

**Výsledek: 0 output — žádné modifikace** ✅

---

## §10 — Check 9: Acceptance

| Check | Výsledek |
|-------|---------|
| `npx prisma validate` | ✅ "Schema valid 🚀" (ověřeno lokálně s dummy DATABASE_URL) |
| `npx tsc --noEmit` | ✅ 0 errors (ověřeno lokálně) |
| `npm run lint` | ✅ 0 errors, 555 warnings (pre-existing `.next` bundle noise, žádné nové z #184) |
| `npm run build` | ✅ impl report: "Compiled successfully in 18.7s" |
| Migration applied na lokální dev DB | ✅ impl report: STOP-1 ritual → cleaned migration aplikována |
| Seed output WHOLESALE_SUPPLIER | ✅ L2836: "Wholesale login: velkoobchod@carmakler.cz / heslo123" |

---

## §11 — Check 10: STOP compliance

| STOP | Popis | Status |
|------|-------|--------|
| STOP-1 tsvector drift | Eskalováno → fresh ACK Option A obdržen → `migrate reset --force` → cleaned migration (odstraněny DROP INDEX artefakty) → druhý reset → baseline indexy intact | ✅ |
| STOP-2 scope creep | Phase B 0 touches, Stripe Connect 0 touches, pricing changes 0 | ✅ |
| STOP-3 protected files | Viz §9 — 0 modifikací | ✅ |
| STOP-4 lint/build | 0 nových errors | ✅ |
| Žádný `--no-verify` / force-push | Impl report neuvádí žádné bypass — atomické commity s řádnými messages | ✅ |
| Self-resolve drift | NE — eskalace provedena, fresh ACK od leada | ✅ |

---

## §12 — Lead decisions (§7 plan) cross-check

| Q | Decision | Implementace | Status |
|---|----------|-------------|--------|
| Q1 | WHOLESALE_SUPPLIER = MARKER only, sdílí PWA s PARTS_SUPPLIER | String comment v User.role, PARTS_SUPPLIER_ROLES extension, **žádný separátní wizard** | ✅ |
| Q2 | warranty = String permissive, max 50, žádný enum | `z.string().max(50).optional()` + Input s placeholder příklady | ✅ |
| Q3 | B-tree + ILIKE, NE tsvector | `@@index([manufacturer])` + `{ contains, mode: "insensitive" }` | ✅ |
| Q4 | Phase B defer kompletně | 0 Phase B touches | ✅ |
| Q5 | Both fields always visible, both optional | Wizard: unconditional inputs. Detail: container hidden jen pokud obojí null, každé pole zvlášť conditional | ✅ |

---

## §13 — Observations

| # | Severity | Popis |
|---|----------|-------|
| OBS-1 | Observation | `PARTS_SUPPLIER_ROLES` definovaný 3× (middleware.ts + 2 API routes) — matches pre-existing `INZERENT_ROLES`/`MAKLER_ROLES` convention v projektu. Refactor by vytvořil novou abstrakci nad 3-file scope rule. Impl report §6 toto dokumentuje. Non-blocking. |
| OBS-2 | Observation | Lint warnings: 555 (pre-existing baseline z `.next` bundlů). Žádné nové warnings z #184 kódu. |
| OBS-3 | Observation | Wizard submit: `details.manufacturer || undefined` — empty string se nepošle jako prázdný string, ale jako undefined → API null. Správné chování (neznečišťuje DB prázdnými řetězci). |

---

## §14 — Verdict

### ✅ PASS — 0 blockers

Commits `9dfadde` → `1b539a3` implementují TASK-020 gap-fix kompletně a správně.

**Klíčové:**
- 3 gapy pokryty: WHOLESALE_SUPPLIER role marker, Part.manufacturer (B-tree + ILIKE), Part.warranty (String permissive)
- Všech 5 Q1-Q5 lead decisions verbatim splněno
- Migration čistá: jen ALTER TABLE + CreateIndex, žádné tsvector side-effects
- Q5 honored: wizard fields always visible, detail container conditional jen pro oba-null
- Protected files: 0 modifikací (webhook, stripe, makler, inzerce, orders, kosik, objednavka)
- STOP-1 ritual: eskalace → fresh ACK → cleaned migration ✅
- TSC 0 errors | lint 0 errors | prisma valid | build ✅
- Seed append-only, wholesale login output viditelný ✅

3 minor observations — žádný blocker. Pipeline GO.
