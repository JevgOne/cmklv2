# QA Report: P1-13 — E2E testy (Playwright)

**Task:** 45  
**Datum:** 2026-04-05  
**Kontroloval:** kontrolor agent  
**Verze:** impl-P1-13.md (HOTOVO)

---

## Vysledek: PASS (s drobnou poznamkou)

Implementace P1-13 splnuje vsechny pozadavky z planu. 7 E2E test souboru s 15 testy pokryva kriticke user flows.

---

## 1. Simplify kontrola

### package.json

- `"@playwright/test": "^1.59.1"` v devDependencies (radek 52)
- `"test:e2e": "playwright test"` (radek 13)
- `"test:e2e:ui": "playwright test --ui"` (radek 14)

### playwright.config.ts

- `testDir: "./e2e"` 
- `fullyParallel: true`
- `forbidOnly: !!process.env.CI` — spravne (zabranuje `.only` v CI)
- `retries: process.env.CI ? 2 : 0` — 2 retries v CI
- `workers: process.env.CI ? 1 : undefined` — serial v CI (stabilita)
- `reporter: process.env.CI ? "github" : "html"` — github reporter pro CI
- `baseURL: process.env.PLAYWRIGHT_BASE_URL || "http://localhost:3000"`
- `trace: "on-first-retry"`, `screenshot: "only-on-failure"`
- 2 projekty: Desktop Chrome + iPhone 14
- `webServer` s auto-start `npm run dev` lokalne, `undefined` v CI

### E2E testy (7 souboru, 15 testu)

| Soubor | Pocet | Pokryti |
|---|---|---|
| `e2e/homepage.spec.ts` | 3 | Title regex /CarMakler/, nav, footer pravni odkazy |
| `e2e/catalog.spec.ts` | 2 | /nabidka, /inzerce — main visible |
| `e2e/auth.spec.ts` | 3 | Login form, chybne udaje (error msg), seed admin prihlaseni |
| `e2e/listing.spec.ts` | 1 | /inzerce — main visible |
| `e2e/contact.spec.ts` | 1 | /kontakt — main + text "Kontakt" |
| `e2e/shop.spec.ts` | 3 | /dily, /shop/katalog, /shop/kosik — main visible |
| `e2e/responsive.spec.ts` | 2 | 375x812 (mobile), 768x1024 (tablet) |

### vitest.config.ts

- `exclude: ['node_modules', '.next', 'playwright', 'e2e']` — e2e pridano

### .gitignore

- `/test-results/`, `/playwright-report/`, `/blob-report/`, `/playwright/.cache/` — vsechny 4 artefakty

---

## 2. Debug kontrola

### Staticka analyza

**Konfigurace:**  
- `reuseExistingServer: true` — lokalne reusuje bezici dev server (spravne)
- `timeout: 120000` pro webServer — 2 minuty na start dev serveru (dostatecne)
- `webServer: undefined` v CI — predpoklada externi server nebo predeploy (typicky pattern)

**Testy:**  
- `auth.spec.ts` radek 25: `await expect(page).not.toHaveURL(/\/login/, { timeout: 10000 })` — 10s timeout pro redirect po prihlaseni (rozumne)
- `auth.spec.ts` radek 22: seed admin `admin@carmakler.cz` / `heslo123` — zavisle na seed datech (ocekavane pro E2E)
- Footer links: `obchodni-podminky`, `ochrana-osobnich-udaju`, `reklamacni-rad` — verifikuje pravni kompletnost

**Potencialni issues:**  
- `listing.spec.ts` testuje `/inzerce` — shodny test jako v `catalog.spec.ts` radek 9. Duplicita, ne bug.
- Testy jsou smoke-level (page load + visibility). Hlubsi interakce (filtry, formular submit, add-to-cart) by vyzadovaly dalsi iteraci.

**Unit testy:**  
- 141/141 PASS — vitest spravne ignoruje e2e/ adresar

---

## 3. Reverzni kontrola (vs plan)

| Pozadavek z planu | Status |
|---|---|
| `@playwright/test` v devDependencies | OK |
| `playwright.config.ts` s 2 projekty (desktop + mobile) | OK |
| 7 test souboru v `e2e/` | OK |
| 15 E2E testu celkem | OK |
| `homepage.spec.ts`: title, nav, footer links | OK |
| `catalog.spec.ts`: nabidka, inzerce | OK |
| `auth.spec.ts`: form, chybne udaje, seed admin | OK |
| `listing.spec.ts`: inzerce stranka | OK |
| `contact.spec.ts`: kontakt stranka | OK |
| `shop.spec.ts`: dily, shop katalog, kosik | OK |
| `responsive.spec.ts`: mobile, tablet | OK |
| `package.json`: test:e2e, test:e2e:ui scripts | OK |
| `vitest.config.ts`: e2e v exclude | OK |
| `.gitignore`: Playwright artefakty | OK |

---

## Shrnuti nalezu

| Priorita | Nalez | Doporuceni |
|---|---|---|
| INFO | `listing.spec.ts` testuje `/inzerce` — duplikace s `catalog.spec.ts` | Kosmeticke, neopravovat |
| INFO | Testy jsou smoke-level (load + visibility) | Ocekavane pro MVP, hlubsi testy v dalsich iteracich |
| INFO | Auth test zavisi na seed datech (`admin@carmakler.cz` / `heslo123`) | Seed musi byt proveden pred E2E testy |

**Kriticke bugy: 0**  
**Blokujici problemy: 0**  
**Implementace pripravena k produkci: ANO**
