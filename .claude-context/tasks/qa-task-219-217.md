# QA Task #219 — PWA Díly C4-C5 (onboarding + polish + diacritics)

**Commit C4:** `51596f3 feat(#210-C4): supplier onboarding flow`
**Commit C5:** `ccc9ae4 chore(#210-C5): loading/error + diacritics fix`
**QA agent:** KONTROLOR
**Datum:** 2026-04-11
**Ref impl:** `.claude-context/tasks/impl-task-218-210-c4c5.md`
**Ref plan:** `.claude-context/tasks/plan-task-210-pwa-dily-100.md` §7-§8

---

## §1 — Check 1: Commit obsah

`git diff 2fa39f3..ccc9ae4 --stat` → **13 souborů, +709/-33** — shoda s impl reportem ✅

### C4 — `51596f3` (7 souborů)

| Soubor | Změna | Status |
|--------|-------|--------|
| `app/(pwa-parts)/parts/onboarding/page.tsx` | NEW, 22 LOC | ✅ |
| `app/(pwa-parts)/parts/onboarding/profile/page.tsx` | NEW, 161 LOC | ✅ |
| `app/(pwa-parts)/parts/onboarding/documents/page.tsx` | NEW, 210 LOC | ✅ |
| `app/(pwa-parts)/parts/onboarding/approval/page.tsx` | NEW, 88 LOC | ✅ |
| `app/(pwa-parts)/parts/onboarding/loading.tsx` | NEW, 10 LOC | ✅ |
| `app/api/auth/supplier-onboarding/route.ts` | NEW, 104 LOC | ✅ |
| `middleware.ts` | EDIT, +5 lines | ✅ |

### C5 — `ccc9ae4` (6 souborů)

| Soubor | Změna | Status |
|--------|-------|--------|
| `app/(pwa-parts)/parts/[id]/loading.tsx` | NEW, 20 LOC | ✅ |
| `app/(pwa-parts)/parts/[id]/edit/loading.tsx` | NEW, 19 LOC | ✅ |
| `app/(pwa-parts)/parts/[id]/error.tsx` | NEW, 37 LOC | ✅ |
| `app/(pwa-parts)/parts/[id]/page.tsx` | EDIT, diacritics | ✅ |
| `app/(pwa-parts)/parts/[id]/edit/page.tsx` | EDIT, diacritics | ✅ |
| `components/pwa-parts/parts/DeletePartDialog.tsx` | EDIT, diacritics | ✅ |

---

## §2 — Check 2: Onboarding 3 kroky

### Krok 1 — Profile (`/parts/onboarding/profile`)

| Prvek | Implementace | Status |
|-------|-------------|--------|
| Navigační wizard (3 kroky) | Kroky 1/2/3 s vizuálním ukazatelem pokroku | ✅ |
| companyName (required) | Input + `companyName.trim() !== ""` | ✅ |
| IČO 8-digit validace | `/^\d{8}$/.test(ico)`, inline error "IČO musí mít přesně 8 číslic", `replace(/\D/g, "").slice(0, 8)` | ✅ |
| phone (required) | Input tel, `phone.trim() !== ""` | ✅ |
| street, city, zip (volitelné) | Inputs, grid 2 col pro city+zip | ✅ |
| description (volitelné) | Textarea | ✅ |
| isValid check | `companyName && icoValid && phone` → disable button | ✅ |
| PATCH /api/auth/supplier-onboarding step=1 | `{ step: 1, data: { companyName, ico, ... } }` | ✅ |
| Redirect po success | `router.push("/parts/onboarding/documents")` | ✅ |

### Krok 2 — Documents (`/parts/onboarding/documents`)

| Prvek | Implementace | Status |
|-------|-------------|--------|
| Krok 1 označen jako dokončený (checkmark) | Zelený kruh s SVG check | ✅ |
| Živnostenský list upload | `POST /api/upload`, `businessRef` input hidden | ✅ |
| Občanský průkaz upload | `POST /api/upload`, `idRef` input hidden | ✅ |
| Upload state | `uploading` flag, "Nahrávám..." | ✅ |
| Potvrzení upload (green check) | Zobrazí název souboru po úspěchu | ✅ |
| PATCH step=2 | `{ step: 2, data: { documents: [url1, url2] } }` | ✅ |
| Disabled dokud oba doc chybí | `disabled={!businessDoc \|\| !idDoc \|\| submitting}` | ✅ |
| Redirect po success | `router.push("/parts/onboarding/approval")` | ✅ |

### Krok 3 — Approval (`/parts/onboarding/approval`)

| Prvek | Implementace | Status |
|-------|-------------|--------|
| Kroky 1+2 označeny jako dokončené | Oba zelené s checkmark SVG | ✅ |
| Animovaný hodiny | `svg ... animate-pulse` | ✅ |
| "Čekáme na schválení" heading | ✅ | ✅ |
| "Obvykle do 24 hodin" | ✅ | ✅ |
| "Co se děje dál?" seznam (3 body) | ✅ | ✅ |
| Email kontakt | `podpora@carmakler.cz` | ✅ |
| "Zpět na úvod" link | `href="/"` | ✅ |

---

## §3 — Check 3: Middleware redirect

```diff
+    // Dodavatel v ONBOARDING stavu → přesměrovat na onboarding (kromě /parts/onboarding)
+    if (token.status === "ONBOARDING" && !pathname.startsWith("/parts/onboarding")) {
+      return NextResponse.redirect(new URL("/parts/onboarding", request.url));
+    }
```

| Aspekt | Výsledek | Status |
|--------|---------|--------|
| Pozice | Uvnitř `/parts` protection bloku, po PARTS_SUPPLIER_ROLES check | ✅ |
| Podmínka | `token.status === "ONBOARDING"` | ✅ |
| Výjimka | `!pathname.startsWith("/parts/onboarding")` — loop prevention | ✅ |
| Cíl | `/parts/onboarding` → router page čte `onboardingStep` a přesměruje na správný krok | ✅ |

---

## §4 — Check 4: API PATCH /api/auth/supplier-onboarding

| Prvek | Implementace | Status |
|-------|-------------|--------|
| Auth check | `!session?.user → 401` | ✅ |
| Role + status check | `PARTS_SUPPLIER_ROLES.includes(role) && status === "ONBOARDING"` → 403 jinak | ✅ |
| Step 1 validation | companyName/ico/phone required, `/^\d{8}$/.test(ico)` | ✅ |
| Step 1 DB update | `onboardingStep: 2`, uloží companyName/ico/phone/cities/bio | ✅ |
| Step 2 validation | `Array.isArray(documents) && documents.length >= 2` | ✅ |
| Step 2 DB update | `documents: JSON.stringify({businessLicense, idCard})`, `onboardingStep: 3`, `status: "PENDING"` | ✅ |
| Neplatný krok | `→ 400` | ✅ |
| PARTS_SUPPLIER_ROLES lockstep | `["PARTS_SUPPLIER", "WHOLESALE_SUPPLIER", "PARTNER_VRAKOVISTE"]` — shodné s middleware a seed | ✅ |

---

## §5 — Check 5: IČO validace

| Místo | Validace | Status |
|-------|---------|--------|
| Frontend (profile/page.tsx) | `/^\d{8}$/.test(ico)` + inline error "musí mít přesně 8 číslic" + `.replace(/\D/g, "").slice(0, 8)` | ✅ |
| Backend (API route.ts) | `/^\d{8}$/.test(ico)` → 400 | ✅ |
| Dvojí validace | Frontend + Backend — správný přístup | ✅ |

---

## §6 — Check 6: Loading.tsx + Error.tsx

| Soubor | Obsah | Status |
|--------|-------|--------|
| `parts/[id]/loading.tsx` | aspect-square skeleton + 3 animated-pulse bloky | ✅ |
| `parts/[id]/edit/loading.tsx` | header + 3 input skeletony | ✅ |
| `parts/[id]/error.tsx` | "use client", `reset` prop, červený icon, "Zkusit znovu" button, "Zpět na díly" link | ✅ |
| `parts/onboarding/loading.tsx` | jednoduchý spinner | ✅ |

**Poznámka:** `error.tsx` prop `error: Error & { digest?: string }` je deklarován ale nepoužit v JSX — standard Next.js vzor, lint nehlásí chybu (unused prop přijatelný). ✅

---

## §7 — Check 7: Diacritics fix

### `app/(pwa-parts)/parts/[id]/page.tsx`

| Text před | Text po | Status |
|-----------|---------|--------|
| "Aktivni" | "Aktivní" | ✅ |
| "Neaktivni" | "Neaktivní" | ✅ |
| "Prodano" | "Prodáno" | ✅ |
| "Rezervovano" | "Rezervováno" | ✅ |
| "Prevodovka" | "Převodovka" | ✅ |
| "Karoserie" | "Karosérie" | ✅ |
| "Interior" | "Interiér" | ✅ |
| "Vyfuk" | "Výfuk" | ✅ |
| "Ostatni" | "Ostatní" | ✅ |
| "Novy" | "Nový" | ✅ |
| "Plne funkcni" | "Plně funkční" | ✅ |
| "Funkcni s vadou" | "Funkční s vadou" | ✅ |
| "Na dily" | "Na díly" | ✅ |
| "Repasovany" | "Repasovaný" | ✅ |
| "Dil nenalezen" | "Díl nenalezen" | ✅ |
| "Dil byl odstranen" | "Díl byl odstraněn" | ✅ |
| "Zpet na moje dily" | "Zpět na moje díly" | ✅ |
| "Vyrobce" | "Výrobce" | ✅ |
| "Zaruka" | "Záruka" | ✅ |
| "OEM cislo" | "OEM číslo" | ✅ |
| "Zobrazeni" | "Zobrazení" | ✅ |
| "Novy" (partType badge) | "Nový" | ✅ |

### `app/(pwa-parts)/parts/[id]/edit/page.tsx`

| Text před | Text po | Status |
|-----------|---------|--------|
| "Dil nenalezen" | "Díl nenalezen" | ✅ |
| "Chyba pri nacitani dilu" | "Chyba při načítání dílu" | ✅ |
| "Ulozeni se nezdarilo" | "Uložení se nezdařilo" | ✅ |
| "Chyba pri ukladani" | "Chyba při ukládání" | ✅ |
| "Zpet na moje dily" | "Zpět na moje díly" | ✅ |
| "Upravit dil" | "Upravit díl" | ✅ |
| "Zrusit" | "Zrušit" | ✅ |

### `components/pwa-parts/parts/DeletePartDialog.tsx`

| Text před | Text po | Status |
|-----------|---------|--------|
| "Smazani se nezdarilo" | "Smazání se nezdařilo" | ✅ |
| "Chyba pripojeni" | "Chyba připojení" | ✅ |
| "Smazat dil?" | "Smazat díl?" | ✅ |
| "Dil bude deaktivovan" | "Díl bude deaktivován" | ✅ |
| "nebude viditelny" | "nebude viditelný" | ✅ |
| "Zrusit" | "Zrušit" | ✅ |
| "Mazu..." | "Mažu..." | ✅ |

**Grep ověření:** `grep` přes všechny 3 soubory pro staré tvary → **0 výsledků** ✅

---

## §8 — Check 8: STOP rules

| STOP | Pravidlo | Ověření | Status |
|------|---------|---------|--------|
| STOP-1 | NE edit API CRUD routes | `git diff ... -- app/api/parts/` → 0 output | ✅ |
| STOP-2 | NE modify Prisma schema | `git diff ... -- prisma/schema.prisma` → 0 output | ✅ |
| STOP-3 | NE install npm packages | `git diff ... -- package.json` → 0 output | ✅ |
| STOP-4 | NE refactor wizard steps | `git diff ... -- PhotoStep/DetailsStep/PricingStep/AddPartWizard` → 0 output | ✅ |
| STOP-5 | Verify JWT fields před middleware edit | `lib/auth.ts`: `status` (L36/68), `onboardingStep` (L41/73) — oba v JWT ✅ | ✅ |
| STOP-7 | Cloudinary env | Upload preset "documents" hardcoded jako string v documents/page.tsx | ⚠️ OBS-1 |
| STOP-8 | >8 files per commit | C4=7, C5=6 | ✅ |

**Protected files (dily, makler, orders, seed):** `git diff 2fa39f3..ccc9ae4` přes protected paths → **0 output** ✅

---

## §9 — Check 9: Build hygiena

| Check | Výsledek |
|-------|---------|
| `npx tsc --noEmit` | ✅ 0 errors (impl report) |
| `npm run lint` | ✅ 0 errors, 554 warnings (beze změny) |
| `npm run build` | ✅ EXIT=0 (impl report) |

---

## §10 — OBS (observace, neblokující)

### OBS-1: Cloudinary upload_preset = hardcoded string

**Soubor:** `app/(pwa-parts)/parts/onboarding/documents/page.tsx` L33  
**Kód:** `formData.append("upload_preset", "documents");`  
**Problém:** Upload preset "documents" je hardcoded string. Ostatní upload flows typicky také hardcodují preset — konzistentní vzor v projektu.  
**Risk:** Nízký — změna presetu vyžaduje edit kódu, ale Cloudinary presets se nemění běžně.  
**Blokátor:** NE — shodný vzor s ostatními upload flows.

---

## §11 — Verdict

### ✅ PASS — 0 blokerů, 1 neblokující OBS

Commity `51596f3` (C4) a `ccc9ae4` (C5) plně implementují scope:

- Onboarding 3 kroky (profile → documents → approval): wizard UI, formuláře, validace, PATCH API ✅
- Middleware redirect ONBOARDING suppliers → `/parts/onboarding` s loop-prevention výjimkou ✅
- API `PATCH /api/auth/supplier-onboarding`: auth + role/status check, IČO 8-digit, DB update, status PENDING ✅
- IČO validace: frontend (regex + input filter) + backend (regex + 400) ✅
- Loading.tsx + error.tsx pro detail/edit routes ✅
- Diacritics fix: všechny texty opraveny v page.tsx, edit/page.tsx, DeletePartDialog.tsx — grep ověřen ✅
- STOP-1/2/3/4/5 dodrženy: 0 CRUD API/schema/deps/wizard změn, JWT fields ověřeny ✅
- Protected files nedotčeny ✅
- Build: TSC 0 errors, lint 0 errors (554 warnings), build EXIT=0 ✅

OBS-1 (hardcoded Cloudinary preset) — konzistentní vzor s ostatními uploads, neblokující.

**Pipeline GO → evžen (code review C4-C5)**
