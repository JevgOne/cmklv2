# QA Task #230 — PWA Partner C1-C4

**Commit C1:** `3273d43 C1: PartnerBottomNav + layout`
**Commit C2:** `fc1f02b C2: Vehicle detail/edit`
**Commit C3:** `42bfd1a C3: Part detail/edit/delete`
**Commit C4:** `bea7003 C4: Order detail + status actions`
**QA agent:** KONTROLOR
**Datum:** 2026-04-11
**Ref impl:** `.claude-context/tasks/impl-task-228-211-c1c4.md`
**Ref plan:** `.claude-context/tasks/plan-task-211-pwa-partner-100.md`

---

## §1 — Check 1: Commit obsah

`git diff ccc9ae4..bea7003 --stat` → **17 souborů, +2185/-15**

Odečteme 2 plan doc soubory (`.claude-context/tasks/*.md`) = **15 source souborů**. Impl report uvádí 14 + 1 cross-module edit (DeletePartDialog.tsx) = 15. ✅

| Commit | Files | Lines | Status |
|--------|-------|-------|--------|
| C1 | 2 (PartnerBottomNav + PartnerLayout) | +176/-11 | ✅ |
| C2 | 4 (vehicles/[id]/page + loading + error, vehicles/page) | +373/-1 | ✅ |
| C3 | 4 (parts/[id]/page + loading + error, parts/page) | +332/-1 | ✅ |
| C4 | 4 (orders/[id]/page + loading + error, orders/page) | +328/-1 | ✅ |
| Cross | 1 (DeletePartDialog z-index fix) | +1/-1 | ✅ |

---

## §2 — Check 2: PartnerBottomNav dual mode (C1)

| Prvek | Implementace | Status |
|-------|-------------|--------|
| Role detection | `session?.user?.role === "PARTNER_VRAKOVISTE"` | ✅ |
| Bazar items | Domů, Vozidla, Přidat (FAB→/vehicles/new), Zájemci, Profil | ✅ |
| Vrakoviště items | Domů, Díly, Přidat (FAB→/parts/new), Objednávky, Profil | ✅ |
| FAB center styling | Orange circle, `w-14 h-14`, `shadow-orange-500/30`, `-mt-3` | ✅ |
| Active state | `pathname === item.href \|\| pathname.startsWith(item.href + "/")` | ✅ |
| Dashboard active guard | Exact match `pathname === "/partner/dashboard"` (prevent over-activation) | ✅ |
| Orange accent | `text-orange-600` pro aktivní item | ✅ |
| Safe area | `pb-[env(safe-area-inset-bottom)]` | ✅ |
| z-index | `z-50` na `<nav>` | ✅ |
| Max 5 items per variant | 5 bazar, 5 vrakoviště | ✅ (STOP-3) |

### PartnerLayout změny

| Prvek | Implementace | Status |
|-------|-------------|--------|
| Hamburger odstraněn | Replaced s centered logo + PARTNER badge | ✅ |
| Fixed top bar | `fixed top-0 left-0 right-0 z-50` | ✅ |
| Padding adjustmenty | `pt-[calc(56px+16px)] lg:pt-4 pb-24 lg:pb-8` | ✅ |
| BottomNav | `<div className="lg:hidden"><PartnerBottomNav /></div>` | ✅ |
| Desktop sidebar | Beze změny | ✅ (STOP-2) |

---

## §3 — Check 3: Vehicle detail/edit (C2)

| Prvek | Implementace | Status |
|-------|-------------|--------|
| Image carousel | Dot navigation, `setImageIndex`, sort by order | ✅ |
| Specs grid | mileage, fuel, transmission, power, VIN, city (conditional) | ✅ |
| viewCount + inquiries | `👁 {viewCount} zobrazení`, `{_count.inquiries} zájemců` | ✅ |
| Inline edit mode | price, mileage, city, description | ✅ |
| Save: PATCH /api/vehicles/[id] | `method: "PATCH"` — shoduje s API route (STOP-5) | ✅ |
| Archive: PATCH /api/vehicles/[id]/status | `{ status: "ARCHIVED" }` | ✅ |
| canArchive guard | `["ACTIVE", "RESERVED"].includes(vehicle.status)` — button hidden jinak | ✅ (STOP-6) |
| vehicles/page.tsx link wrap | `<Link href={\`/partner/vehicles/${v.id}\`}>` | ✅ |
| loading.tsx skeleton | aspect-video + 2×h-6/h-8 + 4 grid skeletons | ✅ |
| error.tsx boundary | "Zkusit znovu" (reset) + "Zpět na vozidla" link | ✅ |

**API metoda ověřena:** `app/api/vehicles/[id]/route.ts` L126 → `export async function PATCH` ✅
**Status API ověřena:** `app/api/vehicles/[id]/status/route.ts` L34 → `export async function PATCH` ✅

---

## §4 — Check 4: Part detail/edit/delete (C3)

| Prvek | Implementace | Status |
|-------|-------------|--------|
| Image carousel | Dot navigation, sort by order | ✅ |
| Category/condition badges | Correct Czech labels (Převodovka, Interiér, Výfuk atd.) | ✅ |
| Price s/bez DPH | `{vatIncluded ? "s DPH" : "bez DPH"}` | ✅ |
| Výrobce, OEM číslo, Záruka | Podmíněné bloky s českou diakritikou | ✅ |
| Kompatibilita | `JSON.parse(compatibleBrands)` + models | ✅ |
| Inline edit | name, price, stock, description | ✅ |
| Save: PUT /api/parts/[id] | `method: "PUT"` — shoduje s API route (STOP-5) | ✅ |
| Delete: DeletePartDialog | cross-module import z `@/components/pwa-parts/parts/DeletePartDialog` | ✅ |
| onDeleted redirect | `router.push("/partner/parts")` | ✅ |
| parts/page.tsx link wrap | `<Link href={\`/partner/parts/${part.id}\`}>` | ✅ |
| loading.tsx skeleton | aspect-video + 2 řádky | ✅ |
| error.tsx boundary | "Zkusit znovu" + "Zpět na díly" | ✅ |

**DeletePartDialog z-index fix:** `z-50 → z-[60]` — správné, dialog musí být nad BottomNav (z-50) ✅

---

## §5 — Check 5: Order detail + status actions (C4)

| Prvek | Implementace | Status |
|-------|-------------|--------|
| Order číslo + datum + status badge | `#{order.orderNumber}`, `toLocaleDateString("cs-CZ")`, Badge | ✅ |
| Buyer info | deliveryName, email, phone | ✅ |
| Items list | name, qty × unitPrice, totalPrice | ✅ |
| Delivery info | deliveryMethod, adresa, trackingNumber | ✅ |
| Price breakdown | itemsTotal + shippingPrice + totalPrice | ✅ |
| PENDING → "Potvrdit objednávku" → CONFIRMED | ✅ | ✅ |
| CONFIRMED → "Odeslat" → tracking input → SHIPPED | Dvoustupňový flow s tracking number | ✅ |
| SHIPPED → "Označit jako doručeno" → DELIVERED | ✅ | ✅ |
| Cancel button | `!["DELIVERED", "CANCELLED"].includes(status)` → "Zrušit objednávku" | ✅ |
| PUT /api/orders/[id]/status | `method: "PUT"` — shoduje s API route | ✅ |
| orders/page.tsx link wrap | `<Link href={\`/partner/orders/${order.id}\`}>` | ✅ |
| loading.tsx skeleton | h-8 + 2×skeletons | ✅ |
| error.tsx boundary | "Zkusit znovu" + "Zpět na objednávky" | ✅ |

**Orders status API ověřena:** `app/api/orders/[id]/status/route.ts` L9 comment + L12 → `export async function PUT` ✅

---

## §6 — Check 6: STOP rules

| STOP | Pravidlo | Ověření | Status |
|------|---------|---------|--------|
| STOP-1 | NE create partner-specific CRUD API | `git diff ... -- app/api/` → 0 output (jen shared APIs volány) | ✅ |
| STOP-2 | NE restructure PartnerLayout | Desktop sidebar beze změny, jen mobile top bar + BottomNav přidány | ✅ |
| STOP-3 | NE >5 items v BottomNav | 5 bazar + 5 vrakoviště | ✅ |
| STOP-4 | NE modify shared API routes | `git diff ... -- app/api/vehicles app/api/parts app/api/orders` → 0 output | ✅ |
| STOP-5 | Vehicle=PATCH, Part=PUT | C2 `method: "PATCH"`, C3 `method: "PUT"`, oba ověřeny v API routes | ✅ |
| STOP-6 | ALLOWED_TRANSITIONS | `canArchive = ["ACTIVE", "RESERVED"].includes(status)` | ✅ |
| STOP-8 | NE install npm packages | `git diff ... -- package.json` → 0 output | ✅ |
| STOP-9 | >10 files per commit | Max 4+1 files (C3 + cross-module fix) | ✅ |

**Protected files** (`dily`, `makler`, `pwa-parts`, `middleware.ts`): `git diff` → 0 output ✅

---

## §7 — Check 7: Diakritika — nové stránky

Nové stránky (C1-C4) mají správnou diakritiku:
- `PartnerBottomNav.tsx`: "Domů", "Vozidla", "Přidat", "Zájemci", "Profil", "Díly", "Objednávky" ✅
- `vehicles/[id]/page.tsx`: "Benzín", "Manuální", "Automat", "Převodovka", "Nájezd", "Výkon", "Uložení se nezdařilo" atd. ✅
- `parts/[id]/page.tsx`: "Výrobce", "OEM číslo", "Záruka", "Převodovka", "Interiér", "Výfuk" atd. ✅
- `orders/[id]/page.tsx`: "Kupující", "Položky", "Doručení", "Sledovací číslo", "Potvrdit objednávku", "Zrušit objednávku" ✅
- `error.tsx` soubory: "Něco se pokazilo", "Zkusit znovu" ✅

---

## §8 — Check 8: Build hygiena

| Check | Výsledek |
|-------|---------|
| `npx tsc --noEmit` | ✅ 0 errors (impl report) |
| `npm run lint` | ✅ 0 errors (555 warnings, +1 od baseline 554) |
| `npm run build` | ✅ EXIT=0 (impl report) |

**Lint +1 warning:** Nárůst z 554 → 555 ze 14 nových souborů — akceptovatelné. ✅

---

## §9 — OBS (observace, neblokující)

### OBS-1: Pre-existing diacritics issues v editovaných list stránkách

**Soubory editované v C1-C4 s pre-existující chybějící diakritikou:**
- `app/(partner)/partner/parts/page.tsx` (editováno v C3): "Moje dily", "Zadne dily", "Zatim nemate zadne dily v systemu."
- `app/(partner)/partner/vehicles/page.tsx` (editováno v C2): "Aktivni" (status filter label)

**Poznámka:** Tyto problémy existovaly před C1-C4 — C1-C4 pouze přidaly Link wrapping, nové problémy nevnesly. Doporučuji opravit v následující iteraci (chore: diacritics partner list pages).

**Blokátor:** NE — nejsou nové regrese C1-C4.

### OBS-2: DeletePartDialog cross-module import

**Soubor:** `app/(partner)/partner/parts/[id]/page.tsx` L11  
**Import:** `from "@/components/pwa-parts/parts/DeletePartDialog"`  
**Poznámka:** Cross-module import (partner používá pwa-parts komponentu). Impl report dokumentuje jako plánovaný vzor (option 2 z plánu). Z-index fix na DeletePartDialog (z-50→z-[60]) zajišťuje správné zobrazení nad PartnerBottomNav. Akceptovatelné pro MVP.  
**Blokátor:** NE

---

## §10 — Verdict

### ✅ PASS — 0 blokerů, 2 neblokující OBS

Commity `3273d43` (C1), `fc1f02b` (C2), `42bfd1a` (C3), `bea7003` (C4) plně implementují scope:

- PartnerBottomNav: dual mode (bazar/vrakoviště), 5 items, FAB, orange accent, safe area ✅
- PartnerLayout: fixed top bar + PARTNER badge, správné padding, hamburger odstraněn ✅
- Vehicle detail: carousel, specs, inline edit (PATCH), archive guard ACTIVE/RESERVED only ✅
- Part detail: carousel, inline edit (PUT), cross-module DeletePartDialog (z-index opravena) ✅
- Order detail: status flow PENDING→CONFIRMED→SHIPPED→DELIVERED, PUT API ✅
- STOP-5: Vehicle=PATCH ✅, Part=PUT ✅, Order=PUT ✅ — všechny ověřeny v API routes
- STOP-4: shared API routes nedotčeny ✅
- STOP-6: canArchive pouze ACTIVE/RESERVED ✅
- Nové stránky: správná diakritika ✅
- Build: TSC 0 errors, lint 0 errors (+1 warning akceptovatelné), build EXIT=0 ✅

**Pipeline GO → evžen (code review C1-C4)**
