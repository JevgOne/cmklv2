# QA Report: P1-16 — Performance Audit

**Task:** 55  
**Datum:** 2026-04-05  
**Kontroloval:** kontrolor agent  
**Verze:** impl-P1-16.md (HOTOVO)

---

## Vysledek: PASS (se 2 nalezky)

Implementace P1-16 pokryva vetsinu pozadavku z planu. Hlavni performance optimalizace (font, lazy load, ISR, cache headers, memo, bundle analyzer, Cloudinary URL) jsou na miste. 2 nalezky k posouzeni.

---

## 1. Simplify kontrola

### Font loading (app/layout.tsx, radky 7-12)

- `weight: ["400", "500", "600", "700", "800"]` -- rezy 300 a 900 odebrane (nepouzivane)
- `display: "swap"` -- pridano, eliminuje FOIT
- `subsets: ["latin", "latin-ext"]` -- zachovano pro ceske znaky

### next/dynamic (app/(web)/nabidka/[slug]/page.tsx, radky 16-20)

- `const PriceHistory = dynamic(() => import("@/components/web/PriceHistory")...)`
- loading placeholder: `<div className="animate-pulse h-64 bg-gray-100 rounded-lg" />`
- **POZNAMKA:** `ssr: false` CHYBI (viz nalez nize)

### Cloudinary URL optimalizace (lib/cloudinary.ts, radky 88-98)

- `getOptimizedUrl(url, width, quality)` export
- Transformace: `w_${width},q_${quality},f_auto,c_fill`
- Guard: `if (!url.includes("res.cloudinary.com")) return url` -- non-Cloudinary URL pass through

### ISR revalidate

| Stranka | revalidate | Status |
|---|---|---|
| `nabidka/page.tsx` | 300 (5 min) | OK |
| `nabidka/[slug]/page.tsx` | 600 (10 min) | OK |
| `shop/produkt/[slug]/page.tsx` | 600 (10 min) | OK |
| `makleri/page.tsx` | 3600 (1 hod) | OK (pre-existing) |
| `marketplace/page.tsx` | 3600 (1 hod) | OK (pre-existing) |
| `chci-prodat/page.tsx` | 3600 (1 hod) | OK (pre-existing) |
| `shop/katalog/page.tsx` | N/A | "use client" -- ISR neaplikovatelne |
| `dily/katalog/page.tsx` | N/A | "use client" -- ISR neaplikovatelne |
| `inzerce/katalog/page.tsx` | N/A | Redirect na /nabidka |

### Cache-Control hlavicky

| Endpoint | Hlavicka | Status |
|---|---|---|
| `api/vehicles` GET | `public, s-maxage=60, stale-while-revalidate=300` | OK |
| `api/listings` GET | `public, s-maxage=60, stale-while-revalidate=300` | OK |
| `api/parts` GET | `public, s-maxage=60, stale-while-revalidate=300` | OK |
| Feed XML endpointy | `public, max-age=3600, s-maxage=3600, stale-while-revalidate=1800` | OK (pre-existing) |
| `api/brokers`, `api/reviews` | N/A | Endpointy neexistuji |
| `api/parts/brands`, `api/parts/categories` | N/A | Endpointy neexistuji |

### Bundle analyzer (next.config.ts + package.json)

- `import withBundleAnalyzer from "@next/bundle-analyzer"` (radek 4)
- `withAnalyze = withBundleAnalyzer({ enabled: process.env.ANALYZE === "true" })` (radky 6-8)
- Wrapper: `withSentryConfig(withAnalyze(withSerwist(nextConfig)))` -- spravne poradi
- Script: `"analyze": "ANALYZE=true next build --webpack"` (radek 15)
- `@next/bundle-analyzer` v devDependencies: `^16.2.2` (radek 53)

### React.memo na list komponentach

| Komponenta | memo | Status |
|---|---|---|
| `VehicleCard.tsx` | `memo(function VehicleCard(...))` | OK |
| `ProductCard.tsx` | `memo(function ProductCard(...))` | OK |
| `PartCard.tsx` | `memo(function PartCard(...))` | OK |
| `OpportunityCard.tsx` | `memo(function OpportunityCard(...))` | OK |
| `RecommendedParts.tsx` | `memo(function RecommendedParts(...))` | OK |

### img -> next/image migrace (sdileno s P1-15)

- **0 `<img>` tagu** v .tsx souborech (grep verifikovano)
- Kompletni migrace

---

## 2. Debug kontrola

### Staticka analyza

**Font:**
- 5 rezu (400-800) je optimalnejsi nez 7. Rez 800 (font-extrabold) zachovan -- 191 vyskytu v codebase.
- `display: "swap"` eliminuje FOIT (Flash of Invisible Text).

**ISR:**
- `shop/katalog` a `dily/katalog` jsou `"use client"` stranky -- export `revalidate` by zpusobil build error. Implementace spravne preskocila.
- `inzerce/katalog` je redirect page na `/nabidka` -- ISR je zbytecne. Spravne preskoceno.

**Cache-Control:**
- Pouzite `s-maxage` (CDN cache) + `stale-while-revalidate` -- spravny pattern pro Vercel/Cloudflare.
- Pouze na GET endpointech -- OK, POST/PUT/DELETE nemaji cache.

**next.config.ts wrapper poradi:**
- `withSentryConfig(withAnalyze(withSerwist(nextConfig)))` -- Sentry outermost (spravne pro source map upload), analyzer uprostred, Serwist innermost.

**Typecheck:** prochazi
**Unit testy:** 141/141 PASS

---

## 3. Reverzni kontrola (vs plan)

| Pozadavek z planu | Status | Poznamka |
|---|---|---|
| Font: odebrat 300+900, pridat display:swap | OK | |
| next/dynamic: PriceHistory (recharts) | CASTECNE | Chybi `ssr: false` (viz nalez) |
| img -> next/image: 22 souboru | OK | Sdileno s P1-15 |
| Cloudinary getOptimizedUrl | OK | |
| ISR: nabidka (300), detail (600) | OK | |
| ISR: shop/katalog, dily/katalog | N/A | Jsou "use client" -- neni mozne |
| ISR: inzerce/katalog | N/A | Je redirect |
| Cache-Control: vehicles, listings, parts | OK | |
| generateStaticParams: nabidka/[slug] | CHYBI | Viz nalez |
| generateStaticParams: shop/produkt/[slug] | CHYBI | Viz nalez |
| generateStaticParams: makler/[slug] | CHYBI | Viz nalez |
| Bundle analyzer: @next/bundle-analyzer | OK | |
| Bundle analyzer: analyze script | OK | |
| React.memo: 5 card komponent | OK | |
| Suspense streaming (volitelne) | NEIMPLEMENTOVANO | Plan oznacil jako "pokrocile, az po zakladech" |

---

## Shrnuti nalezu

| Priorita | Nalez | Detail | Doporuceni |
|---|---|---|---|
| LOW | `ssr: false` chybi u PriceHistory dynamic import | Plan specifikoval `ssr: false` pro recharts (~80KB). Bez nej se recharts stale renderuje na serveru, coz snizuje efekt lazy loadingu. Neni to runtime bug -- recharts zvlada SSR. | Pridat `ssr: false` do options objektu dynamic importu |
| LOW | `generateStaticParams` neimplementovano pro 3 detail stranky | Plan specifikoval pre-rendering top 100 stranek pro `nabidka/[slug]`, `shop/produkt/[slug]`, `makler/[slug]`. Stranky funguji diky ISR revalidate (prvni navsteva je dynamicka, pak cache). | Pridat v dalsi iteraci pro rychlejsi cold-start first visit |
| INFO | Suspense streaming neimplementovano | Plan oznacil jako "pokrocile, az po zakladech". Zakladni optimalizace jsou na miste. | Budouci iterace |

**Kriticke bugy: 0**  
**Blokujici problemy: 0**  
**Implementace pripravena k produkci: ANO**
