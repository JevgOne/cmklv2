# QA Report: P1-10 — Sentry Error Tracking

**Task:** 44  
**Datum:** 2026-04-05  
**Kontroloval:** kontrolor agent  
**Verze:** impl-P1-10.md (HOTOVO)

---

## Výsledek: ✅ PASS

Implementace P1-10 splňuje všechny požadavky z plánu. Sentry je správně integrováno pro client, server i edge runtime.

---

## 1. Simplify kontrola

### package.json

✅ `"@sentry/nextjs": "^10.47.0"` na řádku 22 ✅

### sentry.client.config.ts

✅ DSN: `process.env.NEXT_PUBLIC_SENTRY_DSN` (správný client-side prefix) ✅  
✅ `enabled: process.env.NODE_ENV === "production"` — dev/test bez Sentry ✅  
✅ `tracesSampleRate: production ? 0.1 : 1.0` ✅  
✅ `replaysSessionSampleRate: 0`, `replaysOnErrorSampleRate: production ? 1.0 : 0` ✅  
✅ `ignoreErrors` list: "ResizeObserver loop", "Non-Error promise rejection", "AbortError", "TypeError: Failed to fetch", "TypeError: NetworkError", "TypeError: Load failed" ✅  
✅ `environment: process.env.NODE_ENV` ✅

### sentry.server.config.ts

✅ DSN: `process.env.SENTRY_DSN` (server-side, bez NEXT_PUBLIC) ✅  
✅ `enabled: production` ✅  
✅ `tracesSampleRate: production ? 0.1 : 1.0` ✅

### sentry.edge.config.ts

✅ DSN: `process.env.SENTRY_DSN` ✅  
✅ `enabled: production` ✅  
✅ Shodná konfigurace jako server ✅

### instrumentation.ts

✅ `register()`: `NEXT_RUNTIME === "nodejs"` → importuje `sentry.server.config`, `NEXT_RUNTIME === "edge"` → importuje `sentry.edge.config` ✅  
✅ `onRequestError()`: dynamický import `@sentry/nextjs`, `Sentry.captureRequestError(err, request, context)` ✅  
✅ Správná signatura s `err: { digest?: string } & Error` ✅

### next.config.ts

✅ `import { withSentryConfig } from "@sentry/nextjs"` ✅  
✅ `export default withSentryConfig(withSerwist(nextConfig), {...})` — správné pořadí wrapperů ✅  
✅ `org: process.env.SENTRY_ORG`, `project: process.env.SENTRY_PROJECT`, `authToken: process.env.SENTRY_AUTH_TOKEN` ✅  
✅ `silent: !process.env.SENTRY_AUTH_TOKEN` — tichý režim bez auth tokenu ✅  
✅ `sourcemaps: { disable: !process.env.SENTRY_AUTH_TOKEN }` — source maps jen s auth tokenem ✅  
✅ `autoInstrumentServerFunctions`, `autoInstrumentMiddleware`, `autoInstrumentAppDirectory: true` ✅

### global-error.tsx

✅ `import * as Sentry from "@sentry/nextjs"` ✅  
✅ `import { useEffect } from "react"` ✅  
✅ `error` prop v parametrech (`Error & { digest?: string }`) ✅  
✅ `useEffect(() => { Sentry.captureException(error); }, [error])` ✅  
✅ Existující UI zachováno ✅

### .env.example

✅ Řádky 69-73: `SENTRY_DSN`, `SENTRY_AUTH_TOKEN`, `SENTRY_ORG`, `SENTRY_PROJECT`, `NEXT_PUBLIC_SENTRY_DSN` ✅

---

## 2. Debug kontrola

### Statická analýza

**Graceful degradation:**  
✅ Bez `SENTRY_DSN`/`NEXT_PUBLIC_SENTRY_DSN` → `dsn: undefined` → Sentry se neinicializuje, žádná chyba  
✅ Bez `SENTRY_AUTH_TOKEN` → `silent: true`, `sourcemaps: { disable: true }` → build prochází tiše  
✅ `enabled: process.env.NODE_ENV === "production"` → dev/test prostředí bez Sentry zatížení ✅

**Wrapper pořadí:**  
✅ `withSentryConfig(withSerwist(nextConfig), ...)` — Sentry obaluje Serwist, ne naopak. Správné pořadí — Sentry Webpack plugin musí být outermost ✅

**ignoreErrors:**  
✅ Jen v client config — server chyby by se ignorovat neměly (správné rozhodnutí)

**Implementátorem reportované výsledky:**  
- Typecheck: prochází ✅  
- Unit testy: 141/141 ✅

---

## 3. Reverzní kontrola (vs plán)

| Požadavek z plánu | Status |
|---|---|
| `@sentry/nextjs` v dependencies | ✅ |
| `sentry.client.config.ts`: DSN z NEXT_PUBLIC_SENTRY_DSN, enabled v production | ✅ |
| `sentry.server.config.ts`: DSN, enabled v production | ✅ |
| `sentry.edge.config.ts`: DSN, enabled v production | ✅ |
| `instrumentation.ts`: register() + onRequestError() | ✅ |
| `next.config.ts`: withSentryConfig wrapper kolem withSerwist | ✅ |
| `next.config.ts`: silent, sourcemaps podmíněné SENTRY_AUTH_TOKEN | ✅ |
| `global-error.tsx`: Sentry.captureException(error) | ✅ |
| `.env.example`: SENTRY_DSN, SENTRY_AUTH_TOKEN, SENTRY_ORG, SENTRY_PROJECT, NEXT_PUBLIC_SENTRY_DSN | ✅ |
| ignoreErrors list (ResizeObserver, fetch, AbortError) | ✅ |
| enabled: false mimo produkci | ✅ |

---

## Shrnutí nálezů

**Žádné problémy nalezeny.** Implementace je kompletní a správně konfigurovaná.

**Kritické bugy: 0**  
**Blokující problémy: 0**  
**Implementace připravena k produkci: ANO** (vyžaduje nastavení SENTRY_DSN v .env.local pro aktivaci)
