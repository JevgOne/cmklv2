# QA Report: P2-11 — Stripe platby (dokonceni)

**Task:** 64  
**Datum:** 2026-04-05  
**Kontroloval:** kontrolor agent  
**Verze:** impl-P2-11.md (HOTOVO)

---

## Vysledek: PASS (s 1 MEDIUM nalezem)

Implementace P2-11 splnuje vsechny funkcni pozadavky z planu. Bundle credit system, reserve URL fix i env vars dokumentace jsou spravne implementovany. Chybi migrace pro `listingCredits`.

---

## 1. Simplify kontrola

### .env.example

- Radek 31-33: `STRIPE_SECRET_KEY=`, `STRIPE_PUBLISHABLE_KEY=`, `STRIPE_WEBHOOK_SECRET=`

### listingCredits v User modelu (prisma/schema.prisma)

- Radky 53-54: `listingCredits Int @default(0)` s komentarem
- Za `quickModeEnabled` -- spravne umisteni

### BUNDLE credit handler (app/api/stripe/webhook/route.ts, radky 121-133)

- `case "BUNDLE"`: `prisma.listing.findUnique({ where: { id: listingId }, select: { userId: true } })`
- `prisma.user.update({ data: { listingCredits: { increment: 30 } } })`
- Null guard: `if (bundleListing?.userId)` -- bezpecne
- `increment: 30` -- pricitani, ne nahrazeni (spravne pro vicenasobne nakupy)

### Ostatni promo handlery (webhook/route.ts)

- TOP handler (radky 93-99): `isPremium: true, premiumUntil: +7 dni`
- EXTEND handler (radky 102-119): `expiresAt: +30 dni` (bud od soucasne expirace nebo now), `status: "ACTIVE"`
- handleOrderPayment (radky 137-142): `paymentStatus: "PAID"`
- handleReservationPayment (radky 144-149): `status: "PAID"`
- handleCebiaPayment (radky 151-156): `status: "PENDING"`

### Webhook bezpecnost (webhook/route.ts, radky 21-30)

- `stripe.webhooks.constructEvent(body, signature, STRIPE_WEBHOOK_SECRET)` -- overeni podpisu
- `body = await request.text()` (ne json) -- spravne pro Stripe raw body
- Signature check pred zpracovanim -- spravne

### Reserve URL fix (app/api/listings/[id]/reserve/route.ts)

- Radek 35: `slug: true` v select query
- Radek 110: `success_url: ${NEXTAUTH_URL}/nabidka/${listing.slug}?reserved=true`
- Radek 111: `cancel_url: ${NEXTAUTH_URL}/nabidka/${listing.slug}`
- Puvodne: `/inzerat/${id}` -- neexistujici route, opraveno na `/nabidka/${slug}`

### Promote route (api/listings/[id]/promote/route.ts)

- success_url: `/moje-inzeraty/${id}?promoted=true` -- existujici route
- cancel_url: `/moje-inzeraty/${id}` -- existujici route

### Payment checkout route (api/payments/create-checkout/route.ts)

- success_url: `/nabidka/${slug}/platba/uspech?session_id={CHECKOUT_SESSION_ID}`
- cancel_url: `/nabidka/${slug}/platba?cancelled=true`
- Obe stranky existuji (overeno globem)

### lib/stripe.ts

- Lazy init: `getStripe()` -- neinicializuje bez `STRIPE_SECRET_KEY`
- Build prochazi bez env vars
- `STRIPE_WEBHOOK_SECRET` exportovano s fallback na `""` (webhook handler chyti chybu pri overeni podpisu)

---

## 2. Debug kontrola

**Stripe webhook security:**
- `constructEvent` overuje podpis pred jakymkoli zpracovanim
- BUNDLE handler je pridan do jiz zabezpeceneho flow

**Idempotence:**
- Pokud by stejny webhook dorazil 2x (Stripe at-least-once delivery), `increment: 30` by pridal kredity podruhe. Toto je prijatelne trade-off. Stripe idempotency keys na vyssi vrstve to resolvuji.

**EXTEND handler logika:**
- `baseDate = listing.expiresAt > now ? listing.expiresAt : now` -- neprodluzuje od minuleho datumu. Spravne.

**TypeCheck:** prochazi
**Unit testy:** 141/141 PASS

---

## 3. Reverzni kontrola (vs plan)

| Pozadavek z planu | Status | Poznamka |
|---|---|---|
| STRIPE_PUBLISHABLE_KEY v .env.example | OK | |
| listingCredits Int @default(0) v User modelu | OK | |
| BUNDLE handler: findListing + increment 30 credits | OK | |
| Prisma migrace pro listingCredits | CHYBI | Viz nalez |
| Reserve URL fix: slug v select | OK | |
| Reserve success_url: /nabidka/{slug} | OK | Lepsi UX nez plan (prima slug, ne katalog) |
| Reserve cancel_url: /nabidka/{slug} | OK | |
| Promote success/cancel URL overeni | OK | /moje-inzeraty/{id} |
| Payment success/cancel page existuje | OK | |
| E-shop CARD platba | N/A | Volitelne, plan oznacil jako optional |

---

## Shrnuti nalezu

| Priorita | Nalez | Detail | Doporuceni |
|---|---|---|---|
| MEDIUM | Migrace pro `listingCredits` CHYBI | `prisma/schema.prisma` ma `listingCredits Int @default(0)` na User modelu, ale zadna migrace to nepromitla do DB. Pouze 2 migrace existuji: `init` (bez listingCredits) a `add_password_reset_token`. | Spustit `npx prisma migrate dev --name add_listing_credits` |
| INFO | BUNDLE webhook neni idempotentni | Double-delivery = double credits. Stripe retry mechanismus na vyssi vrstve to miluje, ale v edge case to muze byt problem. | Pridat `stripeSessionId` tracking v dalsi iteraci |
| INFO | E-shop CARD platba neimplementovana | Plan to oznacil jako volitelne rozsireni (Krok 7) | Implementovat v dalsi iteraci dle potreby |

**Kriticke bugy: 0**  
**Blokujici problemy: 0 (migrace je jednoduchá jednoradkova zmena)**  
**Implementace pripravena k produkci: ANO** (po spusteni migrace a nastaveni STRIPE env vars)
