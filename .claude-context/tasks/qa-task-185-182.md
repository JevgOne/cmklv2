# QA Report #185 — TASK-020 Eshop autodíly gap-fix (impl #184)

**Datum:** 2026-04-11
**Agent:** KONTROLOR
**Task:** #185 kontrolor review
**Implementace:** `.claude-context/tasks/impl-task-184-182.md` (commits 9dfadde–1b539a3)
**Plán:** `.claude-context/tasks/plan-task-182-eshop-dily-gap.md`
**Typ:** Reverzní kontrola + Debug kontrola

---

## VERDICT: ✅ SCHVÁLENO — 0 blockerů, 1 minor observation

---

## 1. DEBUG KONTROLA

| Check | Výsledek |
|---|---|
| `npx tsc --noEmit` (source files) | ✅ 0 errors |
| `npm run lint` | ✅ 0 nové errors; 557 warnings (555 baseline + 2 z #211 partner commits — NE z #184) |
| `npm run build` | ✅ `Compiled successfully in 18.6s` |

**Pozn. k tsc:** Jedna pre-existing chyba `e2e/chrome-test-235-c1c7-partner.spec.ts:252` — nesouvisí s #184.

**Pozn. k lint +2 warnings:** Nové 2 warnings jsou z `app/(pwa-partner)/*/onboarding` souborů z #211 commit `4057b4b`. Žádný nový warning z #184 souborů.

**Pre-existing unused imports v #184-dotčených souborech:**
- `Card` v `/dily/[slug]/page.tsx` — existoval od původního TASK-020 commit `629bfc9`
- `Select` v `PricingStep.tsx` — existoval od původního TASK-020 commit `4854e71`
→ Oba pre-existing, NE zavedeny #184.

---

## 2. REVERZNÍ KONTROLA — §3.1 Acceptance Criteria

| # | Kritérium | Výsledek | Kde ověřeno |
|---|---|---|---|
| AC1 | `Part.manufacturer String?` + `Part.warranty String?` v schema | ✅ | `prisma/schema.prisma:906-907` |
| AC2 | `UserRole` comment obsahuje `WHOLESALE_SUPPLIER` | ✅ | `prisma/schema.prisma:21` |
| AC3 | `middleware.ts` `PARTS_SUPPLIER_ROLES` obsahuje `WHOLESALE_SUPPLIER` | ✅ | `middleware.ts:16` |
| AC4 | `createPartSchema` + `updatePartSchema` akceptují `manufacturer` (max 100) + `warranty` (max 50) | ✅ | `lib/validators/parts.ts:18-19` |
| AC5 | `partFilterSchema` + `GET /api/parts` podporují `manufacturer` filter (ILIKE) | ✅ | `lib/validators/parts.ts:51`, `api/parts/route.ts:105-106,134` |
| AC6 | `POST /api/parts` ukládá `manufacturer` + `warranty` z body | ✅ | `api/parts/route.ts:43-44` |
| AC7 | `PUT /api/parts/[id]` akceptuje update polí (přes `updatePartSchema.partial()` + spread) | ✅ | Odvozeno ze schématu (partial dědičnost) |
| AC8 | `POST /api/parts` + `POST /api/parts/import` allowedRoles obsahují `WHOLESALE_SUPPLIER` | ✅ | `api/parts/route.ts:21`, `api/parts/import/route.ts:61` |
| AC9 | `DetailsStep` wizard: `manufacturer` Input field | ✅ | `components/pwa-parts/parts/DetailsStep.tsx:172-173`, initial state `parts/new/page.tsx:25` |
| AC10 | `PricingStep` wizard: `warranty` Input field | ✅ | `components/pwa-parts/parts/PricingStep.tsx:136-137`, initial state `parts/new/page.tsx:33` |
| AC11 | `/dily/[slug]/page.tsx` zobrazuje manufacturer + warranty pokud not null (conditional block) | ✅ | `app/(web)/dily/[slug]/page.tsx:235-254` — container schovaný pokud `!manufacturer && !warranty`, per Q5 |
| AC12 | `/dily/katalog/page.tsx` manufacturer filter input + propaguje do URLSearchParams | ✅ | `app/(web)/dily/katalog/page.tsx:91,110` |
| AC13 | `prisma/seed.ts` obsahuje 1× WHOLESALE_SUPPLIER user + 3 sample Parts s manufacturer + warranty | ✅ | `prisma/seed.ts:1766` (role), `:1784-1785`, `:1808-1809`, `:1832-1833` |
| AC14 | 1× E2E test `e2e/parts-wholesale.spec.ts` | ✅ | Soubor existuje, 4 headed Chromium testy |

**Celkem: 14/14 ✅**

---

## 3. SIMPLIFY KONTROLA

### Kód je čistý — žádné výrazné duplicity ani zbytečná komplexita

- `WHOLESALE_SUPPLIER` přidán konzistentně na 3 místech (middleware + 2 API routes) — odpovídá existujícímu lockstep patternu `INZERENT_ROLES`/`MAKLER_ROLES`/`BUYER_ROLES`
- manufacturer/warranty ILIKE pattern konzistentní s existujícím `name`, `oemNumber`, `partNumber` filtry
- Detail page: conditional `{(part.manufacturer || part.warranty) && ...}` container je správný (Q5: wizard always shows, detail only if data)

---

## 4. MINOR OBSERVATION

### OBS-1 — Middleware test nebyl rozšířen o WHOLESALE_SUPPLIER

**Závažnost:** Nízká (non-blocker)

**Popis:** `__tests__/middleware.test.ts` v sekci `parts-supplier auth guard` testuje pouze `PARTS_SUPPLIER` role (`pustí PARTS_SUPPLIER na parts stránky` na ř. 259). Test pro `WHOLESALE_SUPPLIER` nebyl přidán, přestože plán říká:
> „Implementator čte test file při patch" + „pokud je tam hardcoded role list, přidat WHOLESALE_SUPPLIER do allow-list testu"

**Proč non-blocker:**
- Middleware kód je správný (`PARTS_SUPPLIER_ROLES` na ř. 16 obsahuje `WHOLESALE_SUPPLIER`)
- E2E test `T1: WHOLESALE_SUPPLIER login + middleware /parts gating` toto pokrývá end-to-end
- Plán §3.1 AC3 je splněn (middleware kód, NE test)

**Doporučení pro evzena nebo test-chrome:** Rozšíření middleware unit testu je "nice-to-have" v Fázi B nebo samostatném chore, ne blocker pro pipeline.

---

## 5. STOP RULES AUDIT

| STOP | Dodržen? |
|---|---|
| STOP-1: tsvector drift → eskalace → Option A ACK | ✅ Per §3 impl reportu, eskalace provedena, ACK obdržen |
| STOP-2: pre-HOTOVO (lint + build + smoke) | ✅ Lint + build clean, smoke URLs v impl reportu |
| STOP-3: scope creep | ✅ Žádné Phase B touches, žádné protected system edity |
| STOP-4: §7 decisions před startem | ✅ Všech 5 Q decisions vyplněno v plánu před IMPL |

---

## 6. PROTECTED SYSTEMS — Nedotčeno

Ověřeno greply a git diff commitů A-G:
- ✅ `app/api/stripe/webhook/route.ts` — nedotčeno (#88a commission split)
- ✅ `components/admin/partners/` — nedotčeno (#88a commission slider)
- ✅ `app/(pwa-parts)/parts/profile/` — nedotčeno (#161 Stripe Connect)
- ✅ `app/api/listings/*`, `/inzerce/*`, `/nabidka/*` — nedotčeno (TASK-019)
- ✅ `app/api/orders/*` — nedotčeno (#19 order emails)

---

## 7. SOUHRN

| Kategorie | Výsledek |
|---|---|
| Acceptance criteria | 14/14 ✅ |
| Blokerů | 0 |
| TypeScript errors (source) | 0 |
| Lint errors | 0 |
| Build | ✅ Compiled successfully |
| Minor observations | 1 (middleware test coverage) |

### ✅ SCHVÁLENO — pipeline pokračuje #186 evzen-the-king impl review
